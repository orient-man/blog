## 1. Fix QuotePost Component

- [x] 1.1 T001 (P1) In `src/components/QuotePost.tsx`, replace the outer `<blockquote>` element with a `<div>`
- [x] 1.2 T002 (P1) Remove `italic` and `text-lg` classes from the wrapper element

## 2. Verification

- [x] 2.1 T003 (P1) Run `npm run build` and confirm no build errors
